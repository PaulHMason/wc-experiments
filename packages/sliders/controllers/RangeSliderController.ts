import { ReactiveControllerHost } from 'lit';
import { BaseSliderController } from './BaseSliderController.js';

export class RangeSliderController extends BaseSliderController {
    constructor(host: ReactiveControllerHost) {
        super(host);
    }
}
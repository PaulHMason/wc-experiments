{
    "name": "color",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "tsc --build",
        "dev": "tsc --build --watch",
        "start": "web-dev-server --node-resolve --app-index ./index.html --open"
    },
    "dependencies": {
        "lit": "^2.2.6",
        "colors": "*"
    },
    "devDependencies": {
        "tsconfig": "*",
        "@types/node": "^17.0.12",
        "typescript": "^4.5.3",
        "@web/dev-server": "~0.1.32"
    }
}